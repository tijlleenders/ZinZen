export interface TaskItem {
    taskid: string;
    goalid: string;
    title: string;
    duration: number;
    start: string;
    deadline: string;
    goalColor: string;
    parentGoalId: string;
}
